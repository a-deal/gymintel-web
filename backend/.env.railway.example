# Railway Environment Variables Template
# Copy this to Railway's environment variables dashboard

# Database (Railway provides DATABASE_URL automatically)
# DATABASE_URL is auto-populated by Railway PostgreSQL

# API Configuration
ENVIRONMENT=production
DEBUG=false

# Frontend URL for CORS
FRONTEND_URL=https://gymintel.vercel.app

# API Keys (required for gym search functionality)
YELP_API_KEY=your-yelp-api-key
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Security (generate strong keys for production)
SECRET_KEY=generate-a-strong-secret-key
JWT_SECRET_KEY=generate-another-strong-secret-key

# Optional: Mapbox for backend geocoding
MAPBOX_ACCESS_TOKEN=your-mapbox-token
